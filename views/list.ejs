<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <h1>나는 ejs입니다.</h1>
    <h2><%= title %></h2>
    <ul>
      <!-- 값 - <%= %> 태그,코드 - <% %> -->
      <!-- <% for(let i=0;i<list.length;i++){ %>
        <% list.forEach(function(){}) %>
      <li><%= list[i].subject %></li>
    <% } %>
     -->
      <% list.forEach(function(item,idx){ %>
      <!-- <li><a href="/detail/<%= item.subject %>"><%= item.subject %></a></li> 제목넘김-->
      <li><a href="/detail/<%= item.no %>"><%= item.subject %></a></li>
      <% }) %>
    </ul>
  </body>
</html>
